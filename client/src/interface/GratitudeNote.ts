export interface IGratitudeNote {
  id: string;
  icon: string;      // ví dụ: "❤️" | "💌"
  title: string;
  message: string;
  x: string;         // ví dụ: "40%"
  y: string;         // ví dụ: "25%"
}
